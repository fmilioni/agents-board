node_modules
dist
.nuxt
.output
.nitro
.cache
.data
.vite

# env
.env
.env.local
.env.*.local

# logs
*.log
logs

# OS
.DS_Store
Thumbs.db

# IDE
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Docker volumes
docker/data

# Local, non-committed project memory (board binding, deployment-specific config)
.agents-board.local.md
# Legacy pre-rename binding may still contain private clone-specific settings.
.claude-organizer.local.md
# Legacy Claude Code binding may still contain private clone-specific settings.
CLAUDE.local.md

# Codex worktrees (local, per-task isolation)
.codex/worktrees/
# Legacy Claude Code worktrees remain local for compatibility.
.claude/worktrees/

# Per-machine Claude Code settings (personal permission allowlist)
.claude/settings.local.json
